#include <stdio.h>

/**
 * main- starts execution and termination of prog
 * Return: 0 if executed succesfully
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
