#include <stdio.h>
/* library header */
int main(void) {
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
	/*puts used in place of printf
	 * then moves to next line
	 */
}
