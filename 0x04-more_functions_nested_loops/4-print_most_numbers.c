#include "main.h"

/**
 * print_most_numbers - prints numbers from 0 to 9
 * followed by a new line
 */

void print_most_numbers(void)
{
	int ch;

	for (ch = 48; ch < 58; i++)
	{
		if (ch != 50 && ch != 52)
		{
			_putchar(ch);
		}
	}

	_putchar('\n');
}
